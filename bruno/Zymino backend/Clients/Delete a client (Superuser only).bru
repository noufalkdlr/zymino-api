meta {
  name: Delete a client (Superuser only)
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/clients/:id/
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

example {
  name: 204 Response
  description: No response body
  
  request: {
    url: {{baseUrl}}/api/clients/:id/
    method: DELETE
    mode: none
    params:path: {
      id: 
    }
  }
  
  response: {
    status: {
      code: 204
      text: No Content
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
